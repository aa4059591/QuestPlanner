# 3. Design
# QuestPlanner

<img width="1512" height="1512" alt="QuestPlanner" src="https://github.com/user-attachments/assets/f21c0824-b9ac-4d59-9903-cd4899b7f9dc" />


**Student No:** [22413568]

**Name:** [이윤지]

**GitHub Repository:** <https://github.com/aa4059591/QuestPlanner>

### [ Revision history ]
| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026/06/02 | 1.00 | First draft | [] |
| 2026/06/06 | 1.10 | 클래스 추가 | [] |
---

= Contents =
1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

## 1. Introduction


본 문서는 사용자의 일상 일정 관리에 RPG 게임의 성장 요소를 결합한 프로그램인 'QuestPlanner'의 Design 명세서이다. 앞서 진행된 요구사항 분석 내용을 바탕으로 구상한 기능들을 실제 자바 코드로 구현하기 위해 Class Diagram, Sequence Diagram, State Machine Diagram을 설계하는 것을 목적으로 한다.

본 프로젝트에서 핵심 기능을 살리기 위해 집중한 설계 주안점은 다음과 같다.

1. **실시간 경험치 연동 및 화면 분리 설계**: 사용자가 화면 때문에 몰입이 깨지지 않도록 다크 모드 기반의 현대적인 UI 테마를 채택한다. 특히 핵심 재미 요소인 캐릭터 위젯과 오늘의 퀘스트 리스트를 독립된 패널로 설계한다. 사용자가 리스트에서 할 일을 완료하고 체크박스를 누르는 순간, UI 이벤트 리스너가 즉시 작동하여 캐릭터 패널의 경험치 바가 차오르고 레벨 숫자가 올라가도록 구조화한다. 레벨업 시 폴더 내의 진화 단계별 이미지가 실시간 교체되도록 설계하여 사용자가 성취감을 다이렉트로 느끼게 만든다.

2. **편의성을 위한 경량 파일 데이터 저장 구조**: 사용자가 매일 계획을 기록하고 프로그램을 껐다 켤 때마다 데이터가 날아가거나 수동으로 로드해야 한다면 플래너로서의 가치가 떨어진다. 그렇다고 개인 플래너를 위해 무거운 데이터베이스를 별도로 설치하게 만드는 것도 사용자에게 부담이므로 별도의 프로그램 설치 없이 가볍고 빠르게 구동되도록 텍스트 기반의 `.json` 파일 저장 구조를 설계한다. 프로그램이 닫히거나 데이터가 바뀔 때마다 Jackson 라이브러리가 백그라운드에서 조용히 자동 저장을 수행하도록 설계하여 사용자 편의성을 극대화한다.

3. **멀티스레드 기반의 실시간 동기화**: 혼자만 플래너를 쓰면 쉽게 지치기 때문에 타 유저들과 실시간으로 자극을 주고받는 소셜 기능을 살리는 것이 중요하다. 이를 위해 TCP/IP 소켓 통신을 기반으로 한 서버-클라이언트 아키텍처를 설계한다. 중앙 서버는 여러 사용자가 동시에 접속해도 데이터가 꼬이거나 끊기지 않도록 사용자마다 독립적인 스레드를 할당해 관리한다. 내가 할 일을 마쳐서 레벨업을 하는 순간, 이 변경 신호가 서버를 통해 같은 길드에 있는 모든 팀원들의 화면으로 즉시 순위표가 갱신되도록 설계한다. 이를 통해 유저들이 실시간으로 경쟁하며 계획을 실천할 수 있는 동기를 부여한다.

---

## 2. Class diagram

<img width="3500" height="3820" alt="class" src="https://github.com/user-attachments/assets/52f74a19-3a7d-4c63-b081-34edacefe650" />




### 2.1. ui 패키지

**[1] MainFrame**
* 시스템의 핵심 윈도우 컨트롤러. 전체 UI 컴포넌트와 객체들을 소유하며 실시간 데이터 동기화와 이벤트를 총괄함.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `player` | `Player` | 현재 로그인한 유저 데이터 객체 |
| | `-` | `quests` | `List<Quest>` | 유저의 현재 퀘스트 목록 |
| | `-` | `SAVE_FILE` | `String` | 데이터 저장 로컬 파일 경로 |
| | `-` | `mapper` | `ObjectMapper` | JSON 직렬화/역직렬화 매퍼 |
| | `-` | `currentDate` | `LocalDate` | 화면 표시 기준 날짜 |
| | `-` | `activeClients` | `Map<String, SocialClient>`| 참여 중인 길드방별 소켓 클라이언트 매핑 |
| | `-` | `multiRoomMembers` | `Map<String, Map<String, GameData>>`| 다중 길드방별 소속 멤버 실시간 데이터 캐시 |
| | `-` | `roomCodesList` | `List<String>` | 가입된 방 코드 목록 |
| | `-` | `currentRoomIndex` | `int` | 현재 활성화된 방 인덱스 번호 |
| | `-` | `topSplitPanel` | `JPanel` | 상단 분할 레이아웃 컴포넌트 |
| | `-` | `myPanel` | `JPanel` | 내 정보 표시 패널 |
| | `-` | `guildContainer` | `JPanel` | 길드 정보 컨테이너 패널 |
| | `-` | `statusLabel` | `JLabel` | 플레이어 상태 텍스트 라벨 |
| | `-` | `totalCompletedLabel` | `JLabel` | 퀘스트 누적 완료 횟수 라벨 |
| | `-` | `expBar` | `JProgressBar` | 경험치 진행률 바 |
| | `-` | `imageLabel` | `JLabel` | 캐릭터 이미지 출력 라벨 |
| | `-` | `partyPanel` | `JPanel` | 파티원 정보 출력 패널 |
| | `-` | `guildNameLabel` | `JLabel` | 현재 길드 이름 표시 라벨 |
| | `-` | `dateLabel` | `JLabel` | 날짜 표시 라벨 |
| | `-` | `todoPanel` | `JPanel` | 진행 중인 퀘스트 목록 패널 |
| | `-` | `completedPanel` | `JPanel` | 완료된 퀘스트 목록 패널 |
| | `-` | `summaryLabel` | `JLabel` | 일일 통계 요약 라벨 |
| | `-` | `btnRebirth` | `JButton` | 환생 실행 버튼 |
| **연산<br>(Methods)** | `+` | `MainFrame()` | | 메인 창 초기화 생성자 |
| | `-` | `doLogin()` | `boolean` | 로그인(인증) 절차 수행 |
| | `-` | `saveGameData()` | `void` | 현재 상태를 로컬 JSON으로 저장 |
| | `-` | `isHost(code: String)` | `boolean` | 특정 방의 방장 여부 판별 |
| | `-` | `initSavedRooms()` | `void` | 저장된 기존 가입 방 목록 복구 |
| | `-` | `setupUI()` | `void` | 레이아웃 구성 및 이벤트 리스너 결합 |
| | `-` | `handleLeaveOrDisband()` | `void` | 길드 탈퇴 및 방 폭파 연동 처리 |
| | `-` | `updateGuildVisibility()` | `void` | 가입 길드 유무에 따른 우측 패널 표시 갱신 |
| | `-` | `updateStatus()` | `void` | 레벨, 경험치, 캐릭터 이미지 최신화 |
| | `-` | `updateStatsSummary()` | `void` | 완료된 퀘스트 통계 데이터 리프레시 |
| | `-` | `showMenu()` | `void` | 계정 관리 등 시스템 드롭다운 팝업 |
| | `-` | `logout()` | `void` | 세션 종료 및 로그인 화면으로 리셋 |
| | `-` | `deleteAccount()` | `void` | 세이브 파일 삭제 및 계정 탈퇴 처리 |
| | `-` | `showCollection()` | `void` | 해금 수집품 가이드북 창 오픈 |
| | `+` | `addQuest()` | `void` | 신규 1회성 퀘스트 등록 |
| | `+` | `addRepeatQuest()` | `void` | 반복 루틴 퀘스트 등록 |
| | `-` | `checkTriggerEggOption()` | `void` | 조건 달성 시 알 부화 옵션 팝업 제공 |
| | `-` | `checkAndProcessRebirthCustom(targetRandomType: String)`| `void` | 지정 인자값에 따른 커스텀 환생 절차 수행 |
| | `-` | `editQuest(q: Quest)` | `void` | 선택 퀘스트 타이틀 및 가중치 수정 |
| | `-` | `createQuestMenu(q: Quest)` | `JPopupMenu` | 특정 퀘스트 우클릭 수정/삭제 팝업 생성 |
| | `-` | `cycleGuild(direction: int)`| `void` | 다중 가입된 길드방 목록 좌/우 전환 |
| | `-` | `connectToRoom(code: String, roomName: String, mode: String, isHostMode: boolean)`| `int` | 특정 길드 채널에 소켓 연결 수립 |
| | `-` | `showJoinDialog()` | `void` | 방 개설/입장 코드 입력 다이얼로그 호출 |
| | `+` | `processJoin(roomName: String, password: String, isCreate: boolean)` | `boolean` | 방 생성 및 가입 로직 최종 확정 |
| | `-` | `addGuildLocal(code: String, roomName: String)`| `void` | 로컬 가입 맵 데이터에 길드 추가 |
| | `-` | `removeGuildLocal(code: String)`| `void` | 로컬 가입 맵 데이터에서 길드 삭제 |
| | `-` | `leaveCurrentGuild()` | `void` | 현재 채널 퇴장 및 소켓 소멸 |
| | `-` | `updateGuildUI()` | `void` | 길드방 리스트를 상단 패널에 재렌더링 |
| | `-` | `renderQuests()` | `void` | 기준 날짜의 할 일/완료 목록 화면 배치 |
| | `-` | `handleExpGain(amount: int)`| `void` | 경험치 가산 및 레벨업 모니터링 |
| | `-` | `checkAndProcessRebirth(pendingExp: int)` | `boolean` | 경험치 초과 시 환생 진단 및 이벤트 유도 |
| | `-` | `checkHatchingOrGrowth(oldLevel: int)` | `void` | 레벨 임계치 도달 시 캐릭터 진화 제어 |
| | `-` | `showEventDialog(title: String, message: String)` | `void` | 시스템 알림 메시지 팝업 격리 출력 |
| | `-` | `handleExpLoss(amount: int)`| `void` | 퀘스트 취소에 따른 경험치 삭감 |
| | `-` | `updateAndSync()` | `void` | 로컬 저장 및 서버 동기화 패킷 즉시 방출 |
| | `+` | `main(args: String[])` | `void` | `{static}` 시스템 메인 진입점 |


**[2] LoginDialog**
* 시스템 시작 시 사용자 인증 정보를 입력받는 모달 창.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **연산<br>(Methods)** | `+` | `LoginDialog()` | | 로그인 창 구성 생성자 |
| | `+` | `getNickname()` | `String` | 텍스트 필드 닉네임 반환 |
| | `+` | `getHashedPassword()` | `String` | 암호화된 비밀번호 반환 |
| | `+` | `isNewUser()` | `boolean` | 신규 회원가입 체크 여부 반환 |


**[3] JoinDialog**
* 신규 길드 개설 및 참여 코드를 수집하는 다이얼로그.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **연산<br>(Methods)** | `+` | `JoinDialog(parent: JFrame)`| | 부모 프레임 지정 다이얼로그 생성자 |


**[4] CollectionDialog**
* 유저가 달성 및 해금한 수집품(도감) 내역을 시각화하는 화면.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **연산<br>(Methods)** | `+` | `CollectionDialog(parent: JFrame, unlockedCollection: Set<String>)` | | 해금된 컬렉션 데이터를 넘겨받아 창 생성 |

**[5] DatePickerDialog**
* 사용자의 날짜 입력(Input)을 받고, 해당 일자의 퀘스트 수행 현황을 시각적으로 출력(Output)하는 캘린더 인터페이스.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `selectedDate` | `LocalDate` | 사용자가 선택한 날짜 |
| | `-` | `quests` | `List<Quest>` | 화면 출력을 위한 퀘스트 데이터 |
| **연산<br>(Methods)** | `+` | `DatePickerDialog(parent: JFrame, initialDate: LocalDate, quests: List<Quest>)` | | 초기 화면 구성 및 데이터 입력을 위한 생성자 |
| | `+` | `getSelectedDate()` | `LocalDate` | 최종 선택된 날짜 데이터를 시스템에 반환 |

---

### 2.2. model 패키지

**[5] Player**
* 시스템 사용자의 닉네임, 레벨, 누적 데이터를 보유하는 핵심 도메인 엔티티.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `nickname` | `String` | 고유 아이디 닉네임 |
| | `-` | `password` | `String` | 암호화된 비밀번호 |
| | `-` | `characterType` | `String` | 현재 육성 중인 도트 캐릭터 타입 |
| | `-` | `level` | `int` | 플레이어 현재 레벨 |
| | `-` | `currentExp` | `int` | 누적 경험치 수치 |
| | `-` | `rebirthCount` | `int` | 총 환생 횟수 |
| | `-` | `joinedRooms` | `Map<String, String>` | 가입한 길드 식별용 Map 구조체 |
| | `-` | `unlockedCollection` | `Set<String>` | 해금된 컬렉션 도감 목록 |
| **연산<br>(Methods)** | `+` | `Player(nickname: String, password: String)`| | 초기 캐릭터 도메인 구축 생성자 |
| | `+` | `getNickname()` | `String` | 속성 Getter |
| | `+` | `getPassword()` | `String` | 속성 Getter |
| | `+` | `getCharacterType()`| `String` | 속성 Getter |
| | `+` | `setCharacterType(characterType: String)` | `void` | 속성 Setter |
| | `+` | `getLevel()` | `int` | 속성 Getter |
| | `+` | `setLevel(level: int)` | `void` | 속성 Setter |
| | `+` | `getCurrentExp()` | `int` | 속성 Getter |
| | `+` | `setCurrentExp(currentExp: int)`| `void` | 속성 Setter |
| | `+` | `getRebirthCount()` | `int` | 속성 Getter |
| | `+` | `setRebirthCount(rebirthCount: int)`| `void` | 속성 Setter |
| | `+` | `getJoinedRooms()` | `Map<String, String>` | 속성 Getter |
| | `+` | `getUnlockedCollection()` | `Set<String>` | 속성 Getter |
| | `+` | `addExp(amount: int)` | `void` | 경험치 누적 가산 처리 |
| | `+` | `removeExp(amount: int)`| `void` | 경험치 삭감 처리 |
| | `+` | `getRequiredExp()` | `int` | 다음 레벨업 요구 상한치 계산 |
| | `+` | `getExpPercentage()`| `double` | UI 프로그래스바 렌더링용 백분율 반환 |


**[6] Quest**
* 단일 할 일(투두) 항목의 정보(제목, 일자, 가중치 등)를 저장하는 모델 객체.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `title` | `String` | 퀘스트(할 일) 제목 |
| | `-` | `date` | `String` | 퀘스트 목표 날짜 |
| | `-` | `point` | `int` | 달성 시 획득 가중치 점수 |
| | `-` | `isCompleted` | `boolean` | 퀘스트 달성 완료 유무 |
| **연산<br>(Methods)** | `+` | `Quest(title: String, date: String, point: int)` | | 퀘스트 데이터 초기화 생성자 |
| | `+` | `getTitle()` | `String` | 속성 Getter |
| | `+` | `setTitle(title: String)` | `void` | 속성 Setter |
| | `+` | `getDate()` | `String` | 속성 Getter |
| | `+` | `setDate(date: String)` | `void` | 속성 Setter |
| | `+` | `getPoint()` | `int` | 속성 Getter |
| | `+` | `setPoint(point: int)`| `void` | 속성 Setter |
| | `+` | `isCompleted()` | `boolean` | 속성 Getter |
| | `+` | `setCompleted(completed: boolean)`| `void` | 속성 Setter |


**[7] SaveData**
* 파일로 영속화하여 저장 및 불러오기 위한 최상위 스냅샷 DTO 구조체.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `player` | `Player` | 파일에 직렬화될 유저 객체 |
| | `-` | `quests` | `List<Quest>` | 파일에 직렬화될 퀘스트 리스트 |
| **연산<br>(Methods)** | `+` | `SaveData(player: Player, quests: List<Quest>)` | | 스냅샷 저장을 위한 래퍼 생성자 |
| | `+` | `getPlayer()` | `Player` | 속성 Getter |
| | `+` | `setPlayer(player: Player)` | `void` | 속성 Setter |
| | `+` | `getQuests()` | `List<Quest>` | 속성 Getter |
| | `+` | `setQuests(quests: List<Quest>)`| `void` | 속성 Setter |


**[8] GameData**
* 소켓 네트워크 통신 시 직렬화하여 클라이언트 간 송수신하는 실시간 동기화 패킷 규격.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `command` | `String` | 네트워크 명령 프로토콜 플래그 |
| | `-` | `roomCode` | `String` | 대상 길드방 식별 번호 |
| | `-` | `senderNickname` | `String` | 발송자 아이디 |
| | `-` | `level` | `int` | 발송자 레벨 동기화용 |
| | `-` | `expPercentage` | `double` | 발송자 경험치 진행률 동기화용 |
| | `-` | `isHost` | `boolean` | 방장 권한 보유 여부 |
| | `-` | `rebirthCount` | `int` | 발송자 환생 횟수 동기화용 |
| **연산<br>(Methods)** | `+` | `getCommand()` | `String` | 속성 Getter |
| | `+` | `setCommand(command: String)` | `void` | 속성 Setter |
| | `+` | `getRoomCode()` | `String` | 속성 Getter |
| | `+` | `setRoomCode(roomCode: String)`| `void` | 속성 Setter |
| | `+` | `getSenderNickname()` | `String` | 속성 Getter |
| | `+` | `setSenderNickname(senderNickname: String)`| `void` | 속성 Setter |
| | `+` | `getLevel()` | `int` | 속성 Getter |
| | `+` | `setLevel(level: int)` | `void` | 속성 Setter |
| | `+` | `getExpPercentage()` | `double` | 속성 Getter |
| | `+` | `setExpPercentage(expPercentage: double)`| `void` | 속성 Setter |
| | `+` | `isHost()` | `boolean` | 속성 Getter |
| | `+` | `setHost(isHost: boolean)` | `void` | 속성 Setter |
| | `+` | `getRebirthCount()` | `int` | 속성 Getter |
| | `+` | `setRebirthCount(rebirthCount: int)`| `void` | 속성 Setter |

---

### 2.3. network 패키지

**[9] SocialServer**
* 클라이언트 소켓 연결 요청을 대기하고 다중 방 세션을 중앙 집중형으로 관리하는 서버.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `+` | `PORT` | `int` | `{static, final}` 개방 포트 규격 |
| | `+` | `rooms` | `Map<String, List<ClientHandler>>` | `{static}` 활성화 세션 명부 |
| | `+` | `roomRosters` | `Map<String, Map<String, String>>`| `{static}` 유저 상태값 캐시 |
| | `+` | `disbandedRooms` | `Set<String>` | `{static}` 폭파된 방 블랙리스트 |
| **연산<br>(Methods)** | `+` | `main(args: String[])` | `void` | `{static}` 서버 실행 및 대기 로직 |


**[10] ClientHandler**
* 서버 측에서 개별 클라이언트 유저 세션을 1:1로 전담 통제하는 스레드 파이프라인 모듈.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `socket` | `Socket` | 통신을 위한 물리 소켓 객체 |
| | `-` | `in` | `BufferedReader`| 클라이언트 패킷 수신 스트림 |
| | `-` | `out` | `PrintWriter` | 클라이언트 패킷 송신 스트림 |
| | `-` | `roomCode` | `String` | 스레드가 소속된 방 식별 코드 |
| **연산<br>(Methods)** | `+` | `ClientHandler(socket: Socket)` | | 소켓 바인딩 생성자 |
| | `+` | `run()` | `void` | 패킷 청취 루프 (Thread 재정의) |
| | `-` | `broadcast(message: String)` | `void` | 방 내부 타 유저에게 데이터 전파 |


**[11] SocialClient**
* 클라이언트 측에서 백엔드 서버와 소켓 스트림을 개방하여 패킷을 실시간 송수신하는 통신 래퍼.

| 구분 | 가시성 | 명칭 (이름 및 매개변수) | 반환 타입 | 기타 (Others / 설명) |
| :---: | :---: | :--- | :--- | :--- |
| **속성<br>(Attributes)** | `-` | `socket` | `Socket` | 로컬 가동된 연결 소켓 |
| | `-` | `out` | `PrintWriter` | 서버로 방출할 출력 버퍼 |
| | `-` | `in` | `BufferedReader`| 서버 수신용 입력 버퍼 |
| | `-` | `mapper` | `ObjectMapper` | 직렬화용 JSON 변환기 |
| **연산<br>(Methods)** | `+` | `connect(code: String, player: Player, onData: Consumer<GameData>, mode: String, isHost: boolean)` | `int` | 서버 접속 및 리스너 가동 |
| | `+` | `sendStatus(roomCode: String, player: Player, command: String, isHost: boolean)`| `void` | 내 상태를 패킹해 서버로 송출 |
| | `+` | `disconnect()` | `void` | 스트림 반환 및 소켓 소멸 |

---

## 3. Sequence Diagram

본 장에서는 'QuestPlanner' 시스템의 핵심 Use Case에 대한 Sequence Diagram을 제시하고, 각 객체 간의 동적 상호작용과 메시지 흐름을 상세히 설명한다. 

### 3.1. Account & Data Management (계정 및 데이터 관리)

<img width="853" height="1293" alt="Account   Data Management" src="https://github.com/user-attachments/assets/66c67327-d36f-4f26-8cc6-5ca207a42626" />

시스템 실행 후 사용자의 로그인, 로그아웃, 그리고 계정 탈퇴 과정을 나타내는 시퀀스 다이어그램이다.
* **초기화 및 로그인:** `MainFrame`이 실행되면 저장된 로컬 데이터를 불러와 시스템을 초기화(`initSavedRooms`, `setupUI`)한 뒤, 인증 절차(`doLogin()`)를 시작하여 `LoginDialog` 객체를 생성(`<<create>>`)한다. 사용자가 계정 정보를 입력하면 다이얼로그로부터 닉네임과 해시화된 비밀번호를 가져오고, 신규 유저 여부(`isNewUser()`)를 확인한 뒤 도메인 모델인 `Player` 객체를 생성한다.
* **로그아웃 및 탈퇴 (alt 분기):** `alt` 프레임을 사용하여 두 가지 조건을 분기한다. 로그아웃(`[Logout Selected]`) 시 현재까지의 데이터를 로컬에 저장(`saveGameData()`)하기 위해 `SaveData` 객체를 생성한다. 계정 탈퇴(`[Delete Account Selected]`) 시에는 로컬 세이브 파일을 영구적으로 삭제한다.

### 3.2. Quest Management (할 일 관리)

<img width="639" height="1389" alt="Quest Management" src="https://github.com/user-attachments/assets/f53889ef-c5d2-4e8a-9e7c-29f6edc77362" />

사용자가 일상적인 할 일을 퀘스트 형태로 관리하는 과정을 4가지 조건의 `alt` 프레임 분기로 나타낸다.
* **[Add One-time Quest] / [Add Repeat Quest]:** 사용자가 일반/반복 퀘스트 내용과 보상(Point)을 입력하면 `MainFrame`이 이를 받아 도메인 모델인 `Quest` 객체를 메모리에 생성(`<<create>>`)하고, UI 상에 할 일 목록을 렌더링(`renderQuests()`)한다.
* **[Edit Quest]:** 퀘스트 우클릭 메뉴(`createQuestMenu`)를 통해 내용을 변경하면, 해당 `Quest` 객체의 제목(`setTitle`)과 보상 점수(`setPoint`) 속성을 직접 갱신한 뒤 화면을 재렌더링한다.
* **[Delete Quest]:** 퀘스트 우클릭 메뉴에서 삭제를 선택하면 데이터 파기 후 목록 UI를 갱신(`renderQuests()`)한다.

### 3.3. Quest Completion & Reward (퀘스트 완료 및 보상 처리)

<img width="966" height="1059" alt="Quest Completion   Reward" src="https://github.com/user-attachments/assets/f6405fc5-264b-4654-9817-79a2cb0bb0a2" />

사용자가 할 일을 완수했을 때 경험치(EXP)를 획득하고 레벨업하는 핵심 게이미피케이션(Gamification) 로직이다.
* 사용자가 퀘스트 완료 체크박스를 클릭하면, `MainFrame`은 해당 `Quest` 객체로부터 보상 점수를 가져온다(`getPoint()`).
* 획득한 점수는 즉시 `Player` 객체로 전달되어 경험치가 누적(`addExp()`)되며, 플레이어 스스로 레벨업 상한선 도달 여부를 확인하여 Boolean 값으로 반환(`checkLevelUp()`)한다.
* **레벨업 연출 (opt 프레임):** 반환받은 값이 `true`일 경우에만(`[isLevelUp == true]`), `opt` 조건문 내부의 로직이 가동되어 레벨업 축하 이펙트(`showLevelUpEffect()`)를 시각적으로 출력한다. 이후 플레이어의 상태 바와 퀘스트 목록을 최종 갱신한다.

### 3.4. Guild Connection (네트워크 길드 생성 및 참가)

<img width="847" height="1206" alt="Guild Connection" src="https://github.com/user-attachments/assets/1d734466-9934-4c94-996b-3fc8bdb0a7fd" />

TCP/IP 소켓 통신을 이용하여 다른 사용자와 실시간으로 성취도를 공유하기 위한 길드 방(Room) 생성 및 참가 프로세스다.
* **[Create Guild]:** 사용자가 길드 정보를 입력하면 `JoinDialog`가 `SocialClient` 측으로 방장 권한의 소켓 연결을 요청(`connect`)한다. 서버(`SocialServer`) 승인 후 방 생성 명령(`CREATE`) 패킷을 송신하여 새로운 방 세션을 열고 UI를 갱신한다.
* **[Join Guild]:** 일반 게스트가 방 코드와 비밀번호를 입력하여 연결을 요청한다. `SocialServer`는 자체적인 비밀번호 검증(`Verify Password`) 로직을 거치며, 일치할 경우에만 연결을 승인(`Accept & Open Session`)한다. 이후 방 참가 명령(`JOIN`)을 송신하고 길드 패널을 업데이트한다.

### 3.5. Real-time Data Sync (실시간 랭킹 및 상태 동기화)

<img width="884" height="751" alt="Real-time Data Sync" src="https://github.com/user-attachments/assets/5edd676c-20a0-4945-bca6-d92c71f7f1bd" />

사용자의 경험치나 레벨 등 데이터에 변경이 생겼을 때, 길드원 전체의 화면이 실시간으로 갱신되는 브로드캐스팅(Broadcasting) 과정을 보여준다.
* 퀘스트 완료 등 데이터 변경 이벤트가 발생하면 `MainFrame`은 동기화 함수(`updateAndSync()`)를 호출한다.
* 갱신된 상태값은 `SocialClient`를 통해 `GameData` 패킷으로 직렬화되어 서버로 전송된다.
* 서버(`SocialServer`)는 내부 명부(`Update Room Rosters`)를 최신화한 뒤, 접속 중인 모든 클라이언트에게 패킷을 브로드캐스트(`broadcast`)한다.
* 이를 수신한 각 클라이언트(`SocialClient`)는 메인 프레임으로 데이터를 전달(`onData()`)하여 파티원 목록 패널(`updatePartyPanel()`)을 즉각적으로 갱신한다.

### 3.6. Guild Management (길드 권한 및 세션 관리)

<img width="879" height="1323" alt="Guild Management" src="https://github.com/user-attachments/assets/1bb5255b-9c29-4ea9-99a2-1722a1405840" />

길드 운영에 필요한 3가지 제어 권한 분기 로직이다.
* **[Delegate Host]:** 호스트 유저가 타겟 유저를 선택하여 위임 패킷(`DELEGATE`)을 전송하면, 서버는 권한을 업데이트(`Update Host Rights`)하고 이를 모두에게 브로드캐스트한다.
* **[Disband Guild]:** 호스트가 방을 폐쇄(`DISBAND`)하면, 서버는 해당 길드의 스레드를 파기(`Terminate Server Thread`)하고 해산 명령을 전송한다. 클라이언트는 수신 즉시 로컬 연결을 해제(`handleLeaveOrDisband()`)한다.
* **[Leave Guild]:** 일반 유저가 방을 나갈 경우(`LEAVE`), 서버는 방 명부에서 해당 유저를 삭제(`Remove User from Room`)하고 퇴장 사실을 전파하며, 클라이언트는 로컬 소켓을 닫는다(`leaveCurrentGuild()`).

### 3.7. View Encyclopedia (도감 조회)

<img width="860" height="496" alt="View Encyclopedia" src="https://github.com/user-attachments/assets/8fca8814-9e41-4a0e-9b07-c9dd2806289a" />

사용자가 현재까지 잠금 해제한 캐릭터 도감을 확인하는 조회 로직이다.
* 도감 메뉴를 클릭하면, `MainFrame`은 현재 로그인된 `Player` 객체에게 해금된 수집품 리스트(`getUnlockedCollection()`)를 요청한다.
* 반환받은 컬렉션 데이터(`Set<String>`)를 인자로 전달하여 `CollectionDialog` 객체를 생성(`<<create>>`)한다. 
* 생성된 다이얼로그는 내부 로직(`renderGrid()`)을 통해 전달받은 텍스트(ID) 데이터를 바탕으로 화면에 그리드 UI를 렌더링한다.

---

## 4. State Machine Diagram

본 장에서는 Java 기반 데스크톱 프로그램으로 구현된 QuestPlanner 시스템의 상태 전이를 명확히 정의하기 위해 클라이언트(Client)와 서버(Server)의 상태 머신 다이어그램을 분리하여 설계하였다. 본 도면은 시스템의 실행 주기를 나타내는 복합 상태 내부에, 각 상황별로 객체들이 수행하는 실제 핵심 메서드들을 방사형 전이 흐름으로 매핑하여 기획 요구사항과의 완벽한 추적성을 확보하였다.

### 4.1. Client State Machine

<img width="978" height="844" alt="client" src="https://github.com/user-attachments/assets/86d23a45-3128-4189-ac32-5ca88589f1cd" />

클라이언트 시스템의 라이프사이클과 소켓 연결 유무에 따른 객체 상태 전이를 나타낸다.
* **Login Dialog:** 프로그램 실행 직후의 인증 대기 상태이다. `doLogin()`을 통해 자격 증명을 확인하고 성공 시 로컬 데이터를 불러와(`initSavedRooms()`, `setupUI()`) 메인 화면으로 진입한다.
* **MainFrame Active (Local Mode):** 오프라인 중심의 로컬 환경이다. 사용자는 대기 상태(`Main UI Idle`)에서 다양한 시스템 기능을 호출하며 세부 상태를 순환한다.
  * **Quest Managing:** 퀘스트 생성, 수정, 삭제 작업을 수행한다.
  * **Exp Calculating & Level Up Processing:** 퀘스트 완료 시 점수 및 경험치 누적 상태로 진입한다. 여기서 레벨업 판별(`checkLevelUp()`) 결과가 `true`일 경우, 이펙트 출력 및 캐릭터 진화, 환생을 처리하는 상태로 연계된다.
  * **Collection Viewing:** 도감 호출 시 컬렉션 데이터를 로드하여 화면에 렌더링한다.
* **SocialClient Active (Network Mode):** 길드에 접속하여 소켓 세션이 활성화된 상태이다. 로컬의 데이터 변화나 방 관리 명령을 송신하는 상태(`Data Transmitting`)와, 서버의 브로드캐스트 데이터를 수신하여 패널을 갱신하는 수신 상태(`Data Receiving`)를 비동기적으로 오간다. 통신 종료 시 다시 로컬 모드로 회귀한다.

### 4.2. Server State Machine

<img width="813" height="1050" alt="Server" src="https://github.com/user-attachments/assets/65385517-5c28-449c-b9e0-6fd6682fb823" />

소켓 통신을 기반으로 다중 접속을 중계하는 `SocialServer` 객체의 상태 전이를 나타낸다. 
* **Server Standby:** 서버가 구동되어 클라이언트의 연결 요청을 대기하는 유휴 상태이다. 
* **ClientHandler Active:** 접속 요청이 수락되어 개별 스레드가 할당된 상태이다. 우선 인증 확인(`Verifying Auth`) 상태를 거치며, 패스워드가 유효하지 않으면 세션을 종료하고 대기 상태로 돌아가며, 유효할 경우 본격적인 방 세션으로 진입한다.
* **Room Session Active:** 내부 대기 상태(`Session Idle`)에서 유입되는 명령(Command)에 따라 3가지 핵심 상태로 분기한다.
  * **Roster & Broadcasting:** 방 생성/참여 및 데이터 변동 시 명부를 갱신하고 모두에게 브로드캐스트한다.
  * **Rights Updating:** 방장 위임 시 권한 데이터를 갱신한다.
  * **User Removing:** 유저 퇴장 시 방 목록에서 해당 유저를 제거한다.
  * 방장이 해산 명령을 내리면 해당 스레드를 파기하고 모든 자원을 회수한 뒤 최초의 `Server Standby` 상태로 복귀한다.
 
---

## 5. Implementation Requirements

본 장에서는 QuestPlanner 시스템을 성공적으로 구현하고 구동하기 위한 소프트웨어 및 하드웨어 운영 환경(Operating Environments)을 정의한다. 본 시스템은 중앙 집중형 서버(`SocialServer`)와 다수의 PC 클라이언트(`MainFrame`) 간의 실시간 소켓 통신을 기반으로 하므로, 이에 최적화된 개발 및 실행 환경을 구축한다.

### 5.1. Operating System 
본 시스템은 Java Virtual Machine(JVM) 위에서 동작하도록 설계되어 특정 운영체제에 종속되지 않는 크로스 플랫폼을 지원한다.
* **Client Environment:** 사용자의 PC(Windows, macOS, Linux 등)에 Java Runtime Environment(JRE 17 이상)가 설치되어 있다면, OS 환경과 무관하게 제공된 QuestPlanner.jar를 통해 즉시 동일한 퀄리티로 실행됩니다.

### 5.2. Development Language & Framework 
* **Programming Language:** **Java (JDK 11 이상 권장)**
  * 객체지향 설계 패러다임을 완벽하게 적용하고, 멀티 스레딩(`ClientHandler`) 및 네트워크 I/O를 안정적으로 처리하기 위해 Java를 채택한다.
* **GUI Framework:** **Java Swing & AWT**
  * PC 데스크톱 환경의 화면 구성(`MainFrame`, `LoginDialog`)과 파티원 정보 패널(`updatePartyPanel`), 도감 그리드(`renderGrid()`) 등의 컴포넌트를 렌더링하기 위해 자바 표준 GUI 라이브러리를 사용한다.
* **Network API:** **Java Standard Socket API (`java.net.*`, `java.io.*`)**
  * `SocialServer`와 `SocialClient` 간의 양방향 TCP/IP 통신을 구현하기 위해 사용한다. 서버는 `ServerSocket.accept()`로 접속을 대기하고, 클라이언트는 `Socket` 객체를 통해 세션을 형성한다.
* **Data Serialization:** **JSON Processing Library (Gson 또는 Jackson 등)**
  * 로컬 환경에서의 진행도 영속화(`saveGameData()`) 및 서버-클라이언트 간 데이터 패킷 송수신 시 객체를 직렬화/역직렬화하기 위해 JSON 포맷과 관련 라이브러리를 활용한다.

### 5.3. Development Tools
* **IDE (통합 개발 환경):** IntelliJ IDEA, Eclipse 등 Java 데스크톱 애플리케이션 개발 및 디버깅에 용이한 IDE
* **Modeling Tools:** StarUML, Lucidchart
* **Version Control:** Git & GitHub 

### 5.4. Hardware Requirements 
* **Client PC:** * 데스크톱 GUI 요소를 지연 없이 처리하고, 백그라운드에서 소켓 수신 스레드를 유지할 수 있는 일반적인 사무용 PC 사양 (최소 RAM 4GB 이상, 듀얼 코어 CPU 이상).
* **Server PC:** * 방(Room) 단위로 묶인 여러 클라이언트의 상태 업데이트(UPDATE, DELEGATE, DISBAND)를 실시간으로 받아 메모리에 캐싱하고, 브로드캐스트 패킷을 지속적으로 송출할 수 있는 안정적인 네트워크 대역폭 및 가용 메모리를 갖춘 서버 환경.

---

## 6. Glossary

| 용어 (Term) | 정의 (Definition) |
| :--- | :--- |
| **Local Mode** | 클라이언트가 네트워크 소켓(Socket) 연결 없이 단독으로 실행되어, 모든 처리를 오프라인에서 수행하는 상태. |
| **Network Mode** | 클라이언트가 서버에 연결되어, 다른 사용자들과 진행 상황 및 파티 정보를 실시간으로 공유하는 온라인 상태. |
| **Socket** | 서버와 클라이언트 간의 양방향 TCP/IP 통신을 가능하게 하는 네트워크 연결의 끝점(Endpoint). |
| **Thread** | 프로세스 내에서 독립적으로 실행되는 작업 단위. 본 시스템에서는 다중 접속 처리 및 비동기 데이터 수신을 위해 사용됨. |
| **JSON (JavaScript Object Notation)** | 로컬 데이터를 파일로 저장하거나, 네트워크를 통해 패킷을 전송할 때 데이터를 문자열 형태로 변환하는 경량 포맷. |
| **Serialization** | 메모리 상에 존재하는 시스템 객체를 파일 저장이나 네트워크 전송이 가능한 JSON 등의 형태로 변환하는 과정. |
| **Synchronization** | 특정 클라이언트에서 발생한 데이터의 변화를 서버를 거쳐 다른 파티원들의 화면에 동일하게 일치시키는 작업. |
| **Broadcast** | 서버가 특정 방(Room)에 속한 모든 클라이언트에게 갱신된 최신 상태 데이터를 한 번에 일괄 전송하는 통신 기법. |
| **Callback** | 비동기 네트워크 통신에서 서버로부터 데이터가 수신되었을 때, 시스템이 이를 감지하고 지정된 함수(Consumer 등)를 자동으로 실행하는 메커니즘. |
| **Command Flag** | 클라이언트가 서버에 특정 동작(방 생성, 입장, 위임, 퇴장, 해산 등)을 요청하기 위해 통신 패킷에 포함하여 보내는 식별자. |

---

## 7. References

* 강의자료 : Structural Modeling II, Behavior Modeling I, II
