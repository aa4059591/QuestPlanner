# 3. Design
# QuestPlanner

<img width="1512" height="1512" alt="QuestPlanner" src="https://github.com/user-attachments/assets/f21c0824-b9ac-4d59-9903-cd4899b7f9dc" />


**Student No:** [22413568]

**Name:** [이윤지]

**GitHub Repository:** <https://github.com/aa4059591/QuestPlanner>

### [ Revision history ]
| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026/06/02 | 1.00 | First draft | [] |

---

= Contents =
1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

## 1. Introduction


본 문서는 사용자의 일상 일정 관리에 RPG 게임의 성장 요소를 결합한 프로그램인 'QuestPlanner'의 Design 명세서이다. 앞서 진행된 요구사항 분석 내용을 바탕으로 구상한 기능들을 실제 자바 코드로 구현하기 위해 Class Diagram, Sequence Diagram, State Machine Diagram을 설계하는 것을 목적으로 한다.

본 프로젝트에서 핵심 기능을 살리기 위해 집중한 설계 주안점은 다음과 같다.

1. **실시간 경험치 연동 및 화면 분리 설계**: 사용자가 화면 때문에 몰입이 깨지지 않도록 다크 모드 기반의 현대적인 UI 테마를 채택한다. 특히 핵심 재미 요소인 캐릭터 위젯과 오늘의 퀘스트 리스트를 독립된 패널로 설계한다. 사용자가 리스트에서 할 일을 완료하고 체크박스를 누르는 순간, UI 이벤트 리스너가 즉시 작동하여 캐릭터 패널의 경험치 바가 차오르고 레벨 숫자가 올라가도록 구조화한다. 레벨업 시 폴더 내의 진화 단계별 이미지가 실시간 교체되도록 설계하여 사용자가 성취감을 다이렉트로 느끼게 만든다.

2. **편의성을 위한 경량 파일 데이터 저장 구조**: 사용자가 매일 계획을 기록하고 프로그램을 껐다 켤 때마다 데이터가 날아가거나 수동으로 로드해야 한다면 플래너로서의 가치가 떨어진다. 그렇다고 개인 플래너를 위해 무거운 데이터베이스를 별도로 설치하게 만드는 것도 사용자에게 부담이므로 별도의 프로그램 설치 없이 가볍고 빠르게 구동되도록 텍스트 기반의 `.json` 파일 저장 구조를 설계한다. 프로그램이 닫히거나 데이터가 바뀔 때마다 Jackson 라이브러리가 백그라운드에서 조용히 자동 저장을 수행하도록 설계하여 사용자 편의성을 극대화한다.

3. **멀티스레드 기반의 실시간 동기화**: 혼자만 플래너를 쓰면 쉽게 지치기 때문에 타 유저들과 실시간으로 자극을 주고받는 소셜 기능을 살리는 것이 중요하다. 이를 위해 TCP/IP 소켓 통신을 기반으로 한 서버-클라이언트 아키텍처를 설계한다. 중앙 서버는 여러 사용자가 동시에 접속해도 데이터가 꼬이거나 끊기지 않도록 사용자마다 독립적인 스레드를 할당해 관리한다. 내가 할 일을 마쳐서 레벨업을 하는 순간, 이 변경 신호가 서버를 통해 같은 길드에 있는 모든 팀원들의 화면으로 즉시 순위표가 갱신되도록 설계한다. 이를 통해 유저들이 실시간으로 경쟁하며 계획을 실천할 수 있는 동기를 부여한다.
---

